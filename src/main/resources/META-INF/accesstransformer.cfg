# Widens the client-side block prediction handler so raw block packets can be sent with a
# valid sequence id (see NetworkUtils#sendSequenced). The normal breaking path goes through
# MultiPlayerGameMode#startDestroyBlock and does not need this; it is kept for packet-level
# work where the vanilla entry point is not usable.
public net.minecraft.client.multiplayer.ClientLevel getBlockStatePredictionHandler()Lnet/minecraft/client/multiplayer/prediction/BlockStatePredictionHandler;
